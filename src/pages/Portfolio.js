function Portfolio (){
    return(
        <section>
            <h2>Portfolio</h2>

            <p>
                This page will <code>.map()</code> over my array of projects and use the <code>Project.js</code> components
            </p>

        </section>
    );
}

export default Portfolio;